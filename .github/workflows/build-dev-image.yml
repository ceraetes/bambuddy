# Deprecated: dev image is built by build-integration-dev.yml (merges dev + feature branches).
# Kept for manual workflow_dispatch only during transition.

name: Build dev image (deprecated)

on:
  workflow_dispatch:

permissions:
  contents: read
  packages: write

concurrency:
  group: build-dev-image
  cancel-in-progress: true

jobs:
  redirect:
    name: Use Build integration dev image instead
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "::warning::This workflow is deprecated. Run 'Build integration dev image' instead."
          exit 1
